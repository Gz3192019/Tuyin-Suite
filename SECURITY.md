# 安全说明 Security Policy

## 支持的版本

| 版本 | 支持状态 |
| --- | --- |
| Web 工作台（最新发布） | ✅ 支持 |
| Android（最新发布） | ✅ 支持 |
| 历史版本 | ⚠️ 仅安全修复 |

## 重要提示：隐写 ≠ 加密

本项目的所有模块（LSB 隐写 / RAC 图隐 / 幻影坦克 / 光棱坦克 / 图片混淆）属于**隐写术（Steganography）**，目的是把秘密"藏"在载体中，**不具备密码学意义上的保密性**（LSB 的可选 AES 加密除外，但未设密码时同样无保密性）：

- 幻影坦克、光棱坦克的"隐藏"依赖观察条件（背景色 / 曝光），熟悉原理的人一眼即可识破
- 图片混淆（Arnold 置乱）是像素位置重排，不是加密；迭代次数只是弱密钥，暴力穷举即可还原
- RAC 通过 DCT 系数差编码实现**鲁棒性**（抗压缩/缩放），同样不是加密
- LSB 将数据写进像素末 2 位，肉眼不可见但可被统计检测识别

**请勿将本工具用于保护真正敏感的信息。** 需要保密请使用正规加密方案（如 AES）先行加密，再行隐写。

## 隐私承诺

- Android App 全程本地计算，不联网、不上传任何图片
- 请自行甄别任何要求"上传图片"的第三方镜像站，谨防钓鱼与隐私泄露

## 报告漏洞

发现漏洞或安全隐患，请：

1. 不要公开传播利用细节
2. 通过 GitHub Issues 提交，或向维护者私下联系
3. 描述：影响模块、复现步骤、预期与实际的差异

## 安全更新流程

- 高危问题：7 天内出修复版本，同步更新 `RELEASE_NOTES.md`
- 一般问题：随下一版本修复
