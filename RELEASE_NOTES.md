# Tuyin-Suite · 图隐套件 更新日志

版本号规则：正式版采用 `v1.0.x`，功能迭代期采用 `v0.1.x.beat`；每次发布的 APK 版本号都会迭代，Release 更新日志随版本记录。

---

## v1.0.0（正式版 · 2026-09-26）

图隐套件首个正式版：RAC 图隐 / 幻影坦克 / 光棱坦克 / 图片混淆 四合一聚合 App。

- 包名 `com.setgo.tank`，应用名 StegoTank，versionCode 43
- Compose + Miuix 原生实现（无 WebView），MIUI X 风格
- 主题色全局联动（莫奈取色 + 手动色）、明暗模式、预测性返回、三语界面
- 统一签名，可覆盖安装；支持 GitHub Actions 云端构建发布
