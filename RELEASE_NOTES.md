# StegoTank 更新日志

版本号规则：正式版采用 `v1.0.x`，功能迭代期采用 `v0.1.x.beat`；每次发布的 APK 版本号都会迭代，Release 更新日志随版本记录。

---

## v1.0.4（正式版 · 2026-09-27）

- 新增：**LSB 隐写**——把文字或任意文件藏进 PNG 像素末 2 位（RGB 三通道共 6bit/像素），支持可选 AES 加密；提供实时容量进度池提示，超出容量变红
- 新增：**隐写检测**——扫描图片判断是否疑似藏入数据，多维度检测（LSB 魔数 / LSB 位流随机化 / 幻影 alpha 通道 / 光棱对比 / RAC 分块痕迹），结果分级「确定 / 疑似 / 未发现」，仅作判断提示不保证精确
- 新增：**嵌入历史**——自动记录每次 LSB 嵌入（时间 / 类型 / 内容 / 大小 / 是否加密），最多 50 条，可查看全文与清空；LSB 页与设置页均有入口
- 优化：LSB 检测补查第 1 位并分通道统计（R/G/B），对 2-bit 嵌入的命中率提升
- 优化：幻影通道检测修复 alpha 解码丢失问题，灰度幻影图可准确识别
- 优化：光棱高对比误报收紧（改为亮度两极聚集判据）
- 优化：LSB 提取模式新增「重置」按钮

---

- fix：切换语言后 RAC、幻影坦克二级页及关于页文案不变的问题
- 全 App 界面文案（按钮 / 卡片 / 提示 / 关于页 / 各二级页）接入三语机制（简体 / 繁体 / 英文），语言切换全局生效
- 修正繁体翻译用词（表图 / 光稜坦克等）

---

## v1.0.2（正式版 · 2026-09-26）

- 设置页「语言」行优化：改为点击展开选项列表，交互更直观

---

## v1.0.1（正式版 · 2026-09-26）

- fix：RAC 输出图片大于 20MB 后崩溃的问题
